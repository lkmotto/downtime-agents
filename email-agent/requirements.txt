# DownTime Weekend Email Digest Agent — Python dependencies

# HTTP client (used by all fetchers)
httpx==0.27.0

# Data validation (mirrors backend)
pydantic==2.7.1

# Environment variable loading
python-dotenv==1.0.1

# Shared downtime-backend package (fetchers, scoring, models)
# Replaces the previously bundled backend/ copy
downtime-backend @ git+https://github.com/lkmotto/downtime-backend.git

# Resend SDK (optional, we use httpx directly but include for reference)
# resend==2.3.0

# Async support (stdlib in Python 3.12 — listed for clarity)
# asyncio is part of stdlib
sentry-sdk[asyncio]>=2.0

# OpenTelemetry + Langfuse observability
opentelemetry-api>=1.27.0
opentelemetry-sdk>=1.27.0
opentelemetry-exporter-otlp-proto-http>=1.27.0
