# Core
playwright==1.59.0
httpx==0.28.1
pydantic==2.13.4
python-dotenv==1.2.2

# Playwright stealth — masks automation signals
# (playwright-stealth wraps add_init_script with proven override bundle)
playwright-stealth==2.0.3
sentry-sdk[asyncio]>=2.59.0
